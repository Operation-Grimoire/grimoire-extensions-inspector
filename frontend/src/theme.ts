import { createTheme } from "@mantine/core";

// Dark-only debug tool; keep the indigo accent the old custom CSS used.
export const theme = createTheme({
  primaryColor: "indigo",
  defaultRadius: "md",
  fontFamily: "system-ui, sans-serif",
});
